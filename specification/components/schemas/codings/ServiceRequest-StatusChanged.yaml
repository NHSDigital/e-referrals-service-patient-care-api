title: representation of the date the service request state was set
type: object
required:
  - url
  - valueInstant
properties:
  url:
    type: string
    enum:
      - statusChanged
  valueInstant:
    type: string
    format: date-time
