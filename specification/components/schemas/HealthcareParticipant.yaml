title: HealthcareParticipant
type: object
required:
  - actor
  - status
properties:
  actor:
    type: object
    required:
    - identifier
    properties:
      identifier:
        $ref: identifiers/HealthcareService-identifier.yaml
  status:
    type: string
    enum:
      - accepted
    example: 'accepted'
